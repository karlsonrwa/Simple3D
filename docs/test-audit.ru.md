# Аудит тестов, 15 сентября 2026 — проверяют ли они что-нибудь?

Тот же вопрос и тот же метод, что в аудите GnssSim того же дня: чтением его не
решить — осмысленным выглядит любой тест. Доказательство одно — **мутация**:
сломать код, который тест якобы защищает, прогнать тест и посмотреть, упадёт ли
он. Тест, который остался зелёным, — декорация; а если зелёным остаётся вырезание
целой возможности, то теста просто нет.

Метод записан отдельно, чтобы применять его в других проектах: заметка памяти
`test-audit-playbook` и навык `test-audit` с готовой оснасткой.

## Что измерено

| | |
|---|---|
| Дерево | коммит `cbba9f7` (тогда — вершина ветки `feature/copper-pads`, влитой в `main` 2026-09-17), вместе с несохранёнными правками рабочей копии |
| Базовый прогон | `python tests/run_all.py` — **29 из 29 заданий проходят за 218 с** (25 наборов тестов + 4 механические проверки SKILL и документации); из них 158 с — `test_bend.py` |
| Утверждений | **1198** строк PASS/FAIL в 25 наборах, посчитано прогоном каждого (`test_bend` 220, `test_gui` 144, `test_variant_path` 137, `test_pads` 95, `test_emit` 90; `test_mech` не даёт ни одной) |
| Мутаций | **27 различных**; каждая совпадала в исходнике ровно один раз, писалась побайтно, восстанавливалась в `finally`, после чего сверялась SHA-256. После каждого круга дерево было побайтно равно резервной копии. |
| Итог | **16 поймано, 11 прошли незамеченными** |

Аудит не изменил в репозитории ничего, кроме двух файлов этого отчёта.

## Находки

### 1. Дуги шелкографии не проверяются никакими данными

Три мутации проходят `test_silk.py` насквозь:

- каждая дуга шелкографии заменена своей хордой (ветка дуги в
  `_wire_from_vertices.make` не берётся никогда);
- сторона, в которую выгибается дуга, инвертирована (`_arc_bulges_left`);
- сравнение площадей по каждому полигону в `build_silkscreen` выключено.

Причина — один измеренный факт: **все полигоны шелкографии во всех тестах это
квадраты и квадратное кольцо, и радиус у каждой вершины равен 0.0.**
`test_silk.py` пишет четыре полигона руками (`square()`, `ring()`), а в
`demo/ap-214/demo.json` и во всех четырёх файлах `tests/fixtures/` ключа
`silkscreen` нет вовсе.

То есть шестнадцать прочтений `_pick_convention`, а с ними `_arc_bulges_left`,
`_arc_geometry` и `_arc_edge` — самая продуманная часть `legend.py`, выясненная
измерением на реальной плате с точностью 0.0004 %, — здесь на кривой не
исполняются ни разу. А проверка «площади совпадают с тем, что сообщил Allegro»
(`test_silk [2]`) сравнивает квадрат, площадь которого тест написал сам
(`"area": s * s`), с квадратом, построенным по тем же четырём углам.

Стоит добавить: один полигон с настоящим радиусом и его площадь, взятую из
Allegro, а не вычисленную в тесте. Золотые корпуса это тоже не закрывают: семь
случаев STEP — те же demo и rigid-flex платы, на которых шелкографии нет вовсе.

### 2. Разделяемую деталь не проверяет ничто

`ModelCache.labels_for` импортирует каждую STEP-модель один раз, поэтому десять
одинаковых резисторов стоят одного солида. Если эту связь перерезать — каждый
refdes импортирует модель заново, — **зелёными остаются все четырнадцать
геометрических наборов**, включая регрессию по объёму и числу сущностей против
исходного C++.

Заявляет эту проверку `test_mech.py`. В комментарии написано «Inspect the
assembly tree + count solids to prove the part is shared», в вывод печатается
`solids in shape: N`, а вердикт такой:

```python
ok = (res.components_placed == 2 and not res.components_skipped
      and "symbols_top_mech_test" in txt and "symbols_bot_mech_test" in txt)
```

— число солидов печатается и ни с чем не сравнивается. Три из пяти тегов, для
которых он печатает YES/no (`CR2032_MECH1`, `CR2032_MECH2`, `cap_D8x10mm`), в
вердикт тоже не входят. `test_mech.py` — единственный набор, не идущий через
`_support.check`: измерено — он печатает **ноль** строк PASS/FAIL из 1198 по
всему набору, и весь его результат это одно `ok` выше.

### 3. Экспортёр на SKILL не исполняется ничем: четыре смысловые мутации из пяти проходят

Половина продукта написана на SKILL, и ни один тест его не запускает. Вместо
этого в `tests/skill_transliterations.py` живут питоновские копии процедур, их и
проверяют, плюс грепы по `.il`. Замысел разумный, но он стоит ровно столько,
сколько стоит связь копии с оригиналом. Мутации её и измерили:

| мутация в `skill/` | кто поймал |
|---|---|
| `s3dLayerIsNegative`: `neg = t` → `neg = nil` (негативных слоёв больше нет) | никто |
| `s3dDrillXY`: смещение сверловки применяется с обратным знаком | никто |
| `s3dBoxesMeet`: результат разделяющей оси инвертирован | никто |
| `s3dJsonMerge`: рекурсивная ветка заменена на «взять значение целиком» | никто |
| `s3dJsonQuote`: ветка управляющих символов удалена | никто |
| `simple3d.il`: `--brd-name` переименован в `--board-name` | `test_launcher.py [6]` |
| `simple3d.il`: `s3dConfigRead` переименована | `test_config_merge.py`, `tools/skill_checks.py` |

Яснее всего первый случай виден в `test_neg.py [1]`. Набор начинается с

```python
def is_neg(name, func=None, keys=NEG):
    probe = ((func or "").upper() + " " + (name or "").upper())
    return any(k.upper() in probe for k in keys if k)
```

и дальше делает четырнадцать утверждений про `is_neg` — 14 из 19 в этом наборе —
функцию, написанную тремя строками выше, которую со `skill/s3d_stackup.il:170` не связывает ничто.
Печатает он при этом «[1] транслитерация s3dLayerIsNegative на реальных слоях», а
проверяет сам себя. (Разделы [2] и [3] там настоящие: строят геометрию и меряют
объёмы.)

Две пойманные мутации показывают образец, который стоит скопировать:

- `test_launcher.py [6]` вытаскивает регуляркой **все** флаги `--…` из
  `simple3d.il` и спрашивает настоящий `build_parser()`, знает ли он их. Одна
  проверка — и весь межъязыковой шов закреплён.
- `tools/skill_checks.py` ловит вызов процедуры, не определённой нигде.

Тем же приёмом закрывается и большая часть остального: `test_drill_offset.py`
уже грепает тела `makeSlot` и `symbolReturnPinHoles` на вызов `s3dDrillXY` —
не хватает одного утверждения, связывающего *арифметику* в
`skill/s3d_geometry.il` с питоновской копией.

### 4. `seam_gap` — единственный оракул «свернулось ли» в наборе сгибов можно ослепить

`stepbuilder/bend/plan.py:seam_gap` используется в `test_bend.py` девять раз, и
каждый раз как `seam_gap(plan) < 1e-6`; **ни в одном случае набор не ждёт от него
большого значения**. Если сделать `return 0.0`, все 220 утверждений набора
останутся зелёными.

Это не значит, что сгибы не проверены: тот же набор поймал обе другие мутации
(`_strips_overlap`, всегда возвращающий `False`, и дуги, разбитые на 3 хорды
вместо 8) и делает 62 независимых измерения объёма и габарита. Не показан
именно сам `seam_gap` — его ни разу не видели сообщающим об ошибке. Достаточно
одного случая с заведомо неверно сшитым планом и утверждением про зазор в
миллиметрах.

### 5. Мелочи, найденные чтением

- `tests/test_zones.py:100` — `check("empty zones list behaves as no zones", True)`.
  Условие — литерал; плата выше построена с *удалённым* ключом `zones`, а это
  другой случай, не пустой список.
- `README.md:792` и `README.md:1590` говорят «23 набора тестов». Их 25.

## Что держится

Сказать это нужно прямо, иначе список находок вводит в заблуждение: из 27
мутаций поймано 16, и вырезание любой целой возможности, кроме кэша моделей,
ловит тот набор, который за неё отвечает.

- **`test_pads.py [4]`** — лучший тест в репозитории. Оракул — собственный
  полигон пада, который Allegro сообщает по каждому пину, сохранённый в
  `tests/fixtures/pads_demo.json`; а ещё одно утверждение проверяет, что *сама
  выборка* содержит зеркальный пин, повёрнутый, сквозной и SHAPE — проверку
  покрытия случаев, которой в большинстве наборов нет.
- **`test_emit.py [4]`** грепает настоящий исходник экспортёра *отрицательными*
  структурными утверждениями («ни одна строка не склеивает значение между
  кавычками») — такой греп нельзя удовлетворить случайно.
- **Выходы по коду возврата** у всех наборов идут через `_support.fails`; дефект
  раунда 70 (печатал `MATCH`/`DRIFT` и в обоих случаях выходил нулём) не
  вернулся, и `_support.py` объясняет почему.
- Четыре механические проверки SKILL работают: переименование процедуры в
  `simple3d.il` поймал `skill_checks.py` за секунду.
- `python tools/golden.py --check` сегодня зелёный: 7 случаев, отличий от
  `build/golden.json` нет, 16 с. Стоит помнить, что `run_all.py` ни один из
  золотых корпусов не запускает — они настолько свежи, насколько свеж последний
  ручной прогон.

## Все мутации

Пойманы (16): `board-dedup-off`, `has-solid-always-true`, `restack-above-zero`,
`soldermask-unrecognised`, `zone-levels-no-shift`, `arc-steps-3`,
`merge-config-flat`, `reachable-always`, `pads-mask-data-ignored`,
`pads-never-built`, `legend-never-built`, `strips-never-overlap`,
`dupcut-warning-silenced`, `pad-offset-ignored`, `skill-launcher-flag-renamed`,
`skill-config-not-read`.

Прошли (11): `silk-arc-side`, `silk-arcs-as-chords`,
`silk-area-comparison-off`, `silk-area-check-silenced`, `model-cache-off`,
`seam-gap-zero`, `skill-nothing-is-negative`, `skill-drill-offset-sign`,
`skill-boxes-meet-inverted`, `skill-control-chars-kept`,
`skill-merge-not-recursive`.

Одна из прошедших — не находка: `silk-area-check-silenced` выключил
предупреждение в `_pick_convention`, которое `test_silk.py` и не читает, то есть
мутация била туда, куда набор не целится. Указана, чтобы счёт был честным.

В самом обходе ничего не исправлялось: просили аудит, а не ремонт.
Ремонт пришёл следом и записан ниже.

## Что с этим сделано, 15 сентября 2026

Все находки закрыты, и каждая починка измерена тем же способом, каким была
найдена: мутация делается снова, набор прогоняется, и называется утверждение,
которое её ловит. Все десять прошедших мутаций теперь ловятся.
`python tests/run_all.py` — 30 из 30 за 235 с; наборов стало 26.

| находка | чем закрыта | что падает, если сделать мутацию снова |
|---|---|---|
| 1, дуги шелкографии | `tests/fixtures/silk_demo.json` — шесть полигонов легенды с дугами с демо-платы Cadence и собственная `poly->area` Allegro для каждого; `test_silk [10]`—`[12]` | `silk-arcs-as-chords`: 10 утверждений, худший полигон мимо на 36.3%. `silk-arc-side`: 9, худший 72.7%. `silk-area-comparison-off`: 3, подделанный полигон остаётся неназванным |
| 2, разделяемая деталь | `tests/test_mech.py` переписан: теперь он вообще идёт через `_support.check` и сравнивает плату с 2 копиями и с 5 | `model-cache-off`: 5 утверждений — модель читается 2 и 5 раз вместо одного, а записанных тел становится 11 и 26 вместо 6 |
| 3, шов со SKILL | `tests/test_skill_pins.py` (новый набор), `tools/probes/probe_translit.il`, `tools/skill_answers.py`, `tests/fixtures/skill_answers.json` | все четыре: по два независимых утверждения на каждую — закреплённого выражения в `.il` больше нет, и выведенная из источника проверка не сходится |
| 4, `seam_gap` | `test_bend [7d]`: панель, унесённая от своей полосы на известное расстояние, и две панели, обменявшиеся преобразованиями | `seam-gap-zero`: 6 утверждений, три смещения в мм и порог предупреждения |
| 5, мелочи | `test_zones [5]` строит ту самую плату с пустым списком зон, о которой раньше только говорил; числа исправлены, И `tools/audit_docs.py` теперь выводит их из `run_all.py` | вернуть 23 на место — и аудит документации сообщает об этом за секунду |

Две вещи стоит сказать отдельно от таблицы.

**SKILL теперь исполняется, а не только читается.** Третья находка была
серьёзной: питоновские копии в `tests/skill_transliterations.py` стоили
ровно столько, сколько стоила их связь с оригиналом, а связи не было. Теперь
связей две, потому что каждая по отдельности может протухнуть:

- `test_skill_pins [1]`—`[5]` читают `.il` и сравнивают выражения, несущие
  смысл каждой процедуры, символ в символ — после того как комментарии и
  раскладка нормализованы. Где смысл можно *вывести*, он выводится, а не
  сопоставляется: знак смещения сверловки читается из источника, и копия
  запускается, чтобы проверить, согласна ли она; класс управляющих символов
  читается из `s3dCtrlCharPattern` и сравнивается с предикатом копии по
  каждой кодовой точке.
- `test_skill_pins [7]` сравнивает каждую копию с тем, что ответил
  собственный интерпретатор Allegro. `tools/probes/probe_translit.il`
  вызывает пять процедур в настоящем headless-Allegro на 59 фиксированных
  случаях; `tools/skill_answers.py` превращает этот прогон в
  `tests/fixtures/skill_answers.json` — оракул того же рода, что и
  `pads_demo.json`. Измерено на Allegro 25.1, 26 с: **все 59 совпали.**

  Одно зонд выяснил сам: `sprintf "%c"` в этом SKILL недоступна, так что
  управляющий символ этим способом не построить — но экранированные `\b` и
  `\f` в строковом литерале работают, и оба дают `"a b"`, то есть ветка,
  которую удаляла мутация аудита, действительно исполняется.

`[6]` замыкает круг с другой стороны: каждая строка `# mirrors` в
`skill_transliterations.py` обязана называть процедуру, существующую в
`skill/`, а каждая процедура, которую аудит нашёл незакреплённой, обязана
быть закреплена. Копия, добавленная позже без закрепления, падает там, а не
остаётся тихо без присмотра. Ради этого строки `# mirrors` приведены к одной
машинно-читаемой форме: `# mirrors skill/<файл>.il: proc[, proc ...]  -- проза`.

**`test_neg.py` больше не проверяет сам себя.** Его четырнадцать утверждений
про `is_neg` теперь гоняют общую транслитерацию, которую `test_skill_pins`
привязывает к `skill/s3d_stackup.il:170`.

### Что по-прежнему не покрыто

Сказано прямо, иначе список починок читается как чистая справка.

- Остальные одиннадцать транслитераций закреплены проверкой `[6]` только в
  том смысле, что названная процедура существует. У `s3dVariantFit`, у
  состояний `gdsysGetVariantInfo`, у `s3dSymbolsToExport` и прочих нет ни
  закрепления на уровне выражений, ни записанного ответа Allegro. Оснастка
  для обоих теперь есть — ещё один случай в `probe_translit.il` и ещё один
  вызов `pin(...)`, — так что это работа, а не недостающая идея.
- `run_all.py` по-прежнему не запускает ни один золотой корпус: оба свежи
  ровно настолько, насколько свеж последний ручной прогон.
- `silk-area-check-silenced` остаётся тем, чем был: мутацией, бьющей туда,
  куда ни один набор не целится.

## Второй проход, 17 сентября 2026 (раунд 89)

На следующий день после починки, описанной выше, в другом проекте нашлись
две ошибки в харнессе, которым шёл аудит: Python проверяет `.pyc` по (mtime,
size) исходника, поэтому замена той же длины, записанная в ту же секунду,
может оставить здоровый байткод отвечать за сломанный исходник — или
сломанный байткод пережить восстановление; и мутация, затеняющая строку
ранним `return` над ней, применяется правильно, но оставляет оригинал на
месте, невидимым для любой проверки остатков. Обе теперь обрабатывает общий
харнесс (`~/.claude/skills/test-audit/mutate.py`: очистка кэшей, `-B`, отказ
от замены, содержащей свой оригинал), и весь аудит был прогнан им заново, на
копии дерева, до слияния ветки.

| | |
|---|---|
| Дерево | `03bc47e` (дерево раунда 88) для 27 мутаций, дерево раунда 89 для 21 новой; вся таблица ещё раз на слиянии |
| Харнесс | общий, скопированный в репозиторий как `tools/mutate.py` (замок под `build/`, остальное как в общем) |
| Отказано сразу | 3 — `strips-never-overlap`, `seam-gap-zero`, `pad-offset-ignored`: каждая затеняла строку ранним `return` над ней. Переписаны так, чтобы замена убирала оригинал |
| 27 старых, первый прогон | 20 пойманы, 7 «выжили» |
| Из этих 7 | 6 — вина спецификации, а не тестов: пять SKILL-мутаций и `arc-steps-3` называли наборы, написанные до появления `test_skill_pins.py` (пять) или без `test_bend.py` (одна). Против наборов, которые их заявляют: все 6 пойманы, `test_skill_pins` за 0.2 с. Седьмая, `silk-area-check-silenced`, находкой не была и убрана; `arc-steps-3-bend` слита в `arc-steps-3` |
| Новые мутации | 21: 19 из чтения кода и тестов раундов 86–88 (11 пойманы с первого прогона, 8 выжили — `double-claimed-always-zero`, `drawn-parts-at-copper-height`, `pick-convention-first-wins`, `skill-sweep-finds-nothing`, `skill-boxes-meet-callsite-inverted`, `skill-filter-no-lines`, `readme-count-unstated` и `audit-count-never-stale`, которая меряет свойство инструмента, не заявленное ни одним набором, и в таблицу не вошла) плюс по одной на каждую починку раунда |
| Итог | **48 записей, 48 пойманы** |

Что сказали восемь выживших и что закрыло каждую:

- **`double_claimed` никто не видел сообщающим об ошибке** — та же форма,
  что у `seam_gap` в находке 4: пять вызовов в `test_bend [7b2]`, каждый
  `== 0.0`. Теперь [7d] заставляет одну панель Z-фолда претендовать на весь
  контур и требует назад 80 % — выше порога в 2 %.
- **Третью высоту не мерил никто.** `test_pads [6]` читал z окон и меди
  через `_placement`; детали нарисованных вскрытий через него не идут,
  поэтому 3h, схлопнувшаяся на 2h, проходила. Теперь читается подъём,
  переданный `build_exposed`.
- **Поиску конвенции никогда не приходилось сдвигаться.** Измеренное
  прочтение стоит в списке первым нарочно, и любая выборка давала ему
  победить, так что «поиск», возвращающий первого кандидата, проходил
  `test_silk [10]`. Выборка с перевёрнутым знаком каждого радиуса обязана
  сдвинуть победителя на зеркальное прочтение.
- **Обход меди был закреплён токенами.** `found = nil` (выборка
  выброшена), сопоставление по габариту, применённое наоборот в месте
  вызова, и «lines», выкинутое из фильтра поиска, — всё выживало при всех
  токенах `test_pads [8]` на месте. `test_skill_pins [5b]` закрепляет
  решающие выражения; само поведение — это золотой SKILL-корпус, которому
  нужен Allegro.
- **У цикла подсчёта наборов в аудите документации не было нижней
  границы.** Фраза, выпавшая из регулярного выражения, оставляла на одну
  вещь меньше для сравнения и молчала; теперь README обязан называть число
  по разу на каждом языке.

Ещё два запаха починены без мутации в подтверждение: `test_mech` выводил
число солидов модели из проверяемой сборки (теперь — из файла модели), а
`test_bend [7b3]` индексировал список, который ошибка опустошает, так что
набор падал с исключением вместо провала. И три оставлены как есть, с
записанной причиной: шесть полигонов `silk_demo.json` — те, что различают
прочтения, смещение в выборке, а не в оракуле; `check(..., True)` в
`test_dupcuts` — ветка except пары «обязан бросить»; две границы для
`SHARED_STRIP_RATIO` повторяют измерение, а поведение рядом с ними
закреплено.

Таблица постоянная: `tests/mutations.json`, по записи `{id, file, old, new,
suites, why}` на поломку, применяется `tools/mutate.py` к копии дерева
(20–30 минут на все 48), а `tests/test_mutations.py` — 27-й набор в
`run_all` — за долю секунды спрашивает, применима ли ещё каждая запись:
файл существует, `old` встречается ровно один раз, `new` отличается и не
содержит `old`, ни одна запись не осталась применённой, каждый названный
набор существует. Рефакторинг, ломающий шаблон, проваливается в том же
раунде, а не в конце следующего полного прохода.

### Что по-прежнему не покрыто после второго прохода

- Одиннадцать транслитераций, закреплённых только по имени, и `run_all`,
  не запускающий ни один золотой корпус, — как и 15 сентября.
- Ветка отказа полигонных операций в `s3dCollectExposed` (раунд 89) ни
  разу не исполнялась: ни одна плата до неё не доходит. Её выражения
  закреплены.
- Зеркальный вывод на падстеке со смещённым сверлом: отверстие в площадке и
  отверстие в плате подчиняются разным правилам, и ни один тест не скажет,
  какое из них — правило Allegro, потому что такого падстека нет ни на одной
  плате под рукой (README, *Известные ограничения*).

## Третий проход, 22 сентября 2026 (раунд 90): таблица доказана на копиях

21 сентября работа с мутациями в BaroSim и step2html закрепила правило,
которому харнесс этого репозитория ещё не следовал: мутировать собственную
КОПИЮ дерева, никогда — само дерево. Схема «на месте» — побайтный снимок,
восстанавливаемый после каждой поломки, — требовала файла-замка, проверки
на оставленную мутацию, чистки байткода и правила «никто не правит, пока
идёт прогон», и всё равно стоила четырёх происшествий за неделю в других
проектах. И она измеряла дерево разработчика, в котором есть файлы, каких
нет у клона: две строки BaroSim, неделю числившиеся пойманными, на копии
выжили, одна из них ловилась только благодаря локальному файлу настроек
разработчика, не попадающему в git. У копии есть только то, что под
версионным контролем, плюс то, что харнесс даёт ей намеренно, — и это
честное место для прогона.

| | |
|---|---|
| Харнесс | `tools/mutate.py` — общий харнесс навыка `test-audit` в том виде, в каком его днём раньше взял step2html: у каждого рабочего своя копия дерева в `build/mutants/run-<время>-<pid>/w<i>/`, сделанная без `.git`, `build/`, `input/`, `failed/`, `.claude/`, байткода и `simple3d_config.local.json`; копии идут параллельно, самые долгие наборы первыми; мутированный файл восстанавливается из байтов, прочитанных до записи, и восстановление доказывается SHA-256; `--changed [REV]` выбирает строки, чей файл, набор или запись в таблице отличаются от ревизии, — проверка на день; идентификаторы выбирают строки по подстроке. Замок, проверка на остаток при каждом старте, чистка байткода и запрет на правки ушли вместе со схемой, которой были нужны; осталась проверка, что РАБОЧЕЕ дерево не несёт мутации, — копии наследуют всё, что в нём есть |
| Таблица | те же 48 строк второго прохода, наборы названы путями от корня (`tests/test_pads.py`, `tools/skill_checks.py`) — именно их `--changed` сравнивает с путями, которые называет git |
| Страж | в `tests/test_mutations.py` два новых раздела: копия, которую делает рабочий, несёт каждый файл и каждый набор, названные в строке, побайтно, без байткода и без того, что должно остаться снаружи; и `--changed` выбирает по файлу, набору и строке, и ни по чему больше. 257 проверок примерно за секунду |
| 48 на копиях | **48 из 48 поймано**; 127 файлов в копии (126 отслеживаемых плюс `_occt.py`; харнесс печатает сумму по четырём копиям — 508), копирование 0,2 с; 878 с стены при 2382 с прогонов наборов на четырёх копиях, измерено рядом с двумя полными прогонами и четырьмя стартующими агентами (набор сгибов 254 с в базовом прогоне и 310–337 с на строку против 160 с в одиночестве) |

Что сказали копии: ничего, что скрывало бы дерево. Ни одна строка этой
таблицы не берёт силу из окружения — в корне репозитория нет локального
файла настроек, а все фикстуры, которые читают наборы, под версионным
контролем, — и это факт о таблице, который может установить только копия.
Две вещи о прогоне: при выводе в файл Python буферизует весь журнал до
конца, так что за ходом следят, запуская с `-u`; и полный прогон такого
размера — около пятнадцати минут стены на занятой машине, и большая часть —
шесть строк набора сгибов.

### Таблица выросла: по строке на каждое решение, которое наборы объявляют

step2html днём раньше показал, чего не видит таблица в 48 строк: когда
строки написали по одной на каждое решение, объявленное наборами, из первых
150 выжили 65, и каждая выжившая оказалась слабым тестом. Поэтому четыре
агента (opus, параллельно; у каждого свой непересекающийся набор наборов, и
целит он только в модули, которые эти наборы объявляют; ни код, ни таблицу,
ни что-либо общее не правит) прочитали свои наборы на девять запахов,
написали строки, прогнали их на копиях харнессом, починили каждый слабый тест
в собственных файлах и прогнали строку заново; координатор слил строки
скриптом, который перепроверяет каждую против итогового дерева (id новый,
файл и наборы существуют, `old` ровно один раз, `new` его убирает). Машину
всё это время делили с прогонами мутаций двух других проектов, так что набор
сгибов стоил 260–2125 с за прогон вместо 160, и агент по сгибам прогнал 15
из 26 написанных строк.

| область (агент) | прогнано строк | поймано наборами как были | поймано после правок | отброшено, с замером | в таблице |
|---|---:|---:|---:|---:|---:|
| плата: зоны, слои, режимы, плоская плата, без маски, негативы, вырезы, регрессия → `board`, `stackup`, `contour`, `stepdoc`, `colors`, стадии платы в `core` | 45 | 38 | 44 | 1 | 44 |
| площадки, шелкография, модели: pads, silk, mech, embedded, index → `pads`, `legend`, `models`, стадии шелкографии / площадок / расстановки в `core` | 65 | 47 | 62 | 3 | 62 |
| SKILL, запуск, настройки, чтение, окно: quote, emit, drill offset, skill pins, launch cmd, launcher, variant path, config merge, settings, gui, geom и механические проверки → `skill/*.il`, `simple3d.il`, `settings`, `winplace`, `worker_bridge`, `intermediate`, `__main__`, документы, которые читает аудит | 54 | 26 из 48 | 53 | 1 | 53 |
| свёртка: test_bend → `bend/*`, `core._plan_fold` | 15 | 10 | 13 | 1, и 1 открыта | 13 |
| **вместе** | **179** | **121** | **172** | **6** | **220 вместе с 48** |

**Что сказали выжившие.** Первый прогон пережила 51 строка. Шесть из них
не находки: четыре эквивалентных мутанта (диапазон параметра дуги, который
`GC_MakeArcOfCircle` разворачивает сам; флаг ccw дуги площадки, сдвигающий
точку привязки на 6,7e-16 мм²; знак якоря, который `_walk` перерешает на
стыке — все преобразования совпадают до последней цифры на пяти формах
плана; правило «печатная зона побеждает», достижимое только при
перекрывающихся зонах, которых нет ни в одной фикстуре), одна нацелена на
фразу, о которой ни одна проверка ничего не утверждает, и одна остаётся
открытой ниже. **Сорок пять были слабыми тестами, и каждый починен в тот же
день, в наборе, который объявляет это поведение, и доказан строкой, которая
после этого кусается.** По видам:

- *Проверки, которые не могли упасть.* «layerFunction всё равно побеждает»
  было истинно потому, что имя фикстуры классифицировало её и так
  (`test_modes [1]`); «свёртка — одно тело» было `volume > 0 and not
  IsNull()`, чему удовлетворяет и груда из трёх (`test_bend [3]`); «и говорит
  об этом один раз» искало фразу, которую печатают обе конструкции
  (`test_bend [17]`); диалог предполётной проверки искали по слову
  `axlUIConfirm` где угодно в файле — а его несёт комментарий тремя строками
  выше вызова (`test_launch_cmd [5]`); третья высота была *аргументом*
  подъёма, а не z (`test_pads [6]`).
- *Оракулы, которых не видели падающими.* Заметку `_neutral_ceiling` никто
  не читал; слияние плоской шелкографии судили только по размеру файла.
- *Фикстуры, которые не могут различить.* Каждый стек под рукой замаскирован
  с обеих сторон или ни с одной, так что `mask_sides` мог ответить за другую
  сторону; заглушка свёртки в `test_pads [3]` была сдвигом по z, который
  коммутирует с обоими сомножителями установки; угол символа в демо равен 0;
  `wide_hold` широк по x, а не вдоль сгиба, так что удерживаемый кусок
  кончался на полосе и протяжённость отвечала верно по везению; обе стороны
  шелкографии выключали разом, и цикл по сторонам не входил; две
  замаскированные зоны в `test_pads [9]` стоят на одном z.
- *Решения, которых не объявлял никто.* Весь `component_transform` — порядок
  вращений, система смещения, угол символа, переворот, грань, на которую
  ложится нижняя деталь, поверхность самой зоны (теперь `test_mech [3]`,
  арифметика выписана на бумаге); одна строка `_prepare_stackups`, доходящая
  до `align_stackups`; *положение* датума в режимах плоской платы;
  тринадцать процедур SKILL, закреплённых лишь по имени (`test_skill_pins
  [5c]`–`[5f]`, а `[6]` теперь требует, чтобы каждая зеркалируемая процедура
  была закреплена выражением, — копия, добавленная позже, не останется без
  присмотра); `s3dResolveCadDir`, правило pcb → cad (`test_launch_cmd [6]`);
  фильтр `winplace` «почти экран» (`test_geom [7b]`); полоса, от которой
  отказывается вращение, — и проверка собственного объёма обёртки наконец
  исполняется (`test_bend [17c]`).
- *Наборы, которые падали исключением вместо FAIL.* Незамкнутая проволока
  площадки валила `test_pads` трассировкой; `said[-1]` правила пакета в
  `test_variant_path` падал ровно на той поломке, ради которой существует;
  три строки платы по-прежнему ловятся исключением набора (`test_dupcuts
  [2]`, `test_modes [4]`, `test_plain_modes [2]`) — честный ненулевой выход,
  который обрывает набор на этом месте; отмечено, не изменено.
- *Сама проверяющая машинерия*, три дефекта: проверка `format_version` в
  `tools/audit_docs.py` принимала свободные формы, которыми README описывает
  ИСТОРИЮ («в 11 нет переходных»), так что понижение экспортёра до 11
  проходило, — теперь требуется форма `format_version: N` по разу на язык и
  сравнивается каждое вхождение; `test_gui [9]` при одной мутации доходил до
  модального окна и сидел 31 минуту на 2 % CPU со всем выводом прогона в
  буфере — окно заглушено, «ничего не показано» утверждается; а
  8-секундный дедлайн `test_launch_cmd` на отсоединённый `start` краснел на
  чистой копии загруженной машины, и две строки читались как «выжили» по
  причине, никак не связанной с тестами, — теперь 40 с, а харнесс называет
  такие строки НЕДОКАЗАННЫМИ, а не выжившими (ловушка 8 в его докстринге), с
  таймаутом в три базовых времени набора плюс две минуты, после которого
  дерево процессов набора убивается, а строка — HUNG (ловушка 7).

**Дефектов в коде нет.** Ни одна из 179 мутаций не показала, что Python или
SKILL делают не то; два докстринга утверждают больше, чем подтверждает
замер (`stepdoc.write` говорит, что без `UpdateAssemblies()` документ пуст;
измерено: плата всё равно проходит как свободная фигура, теряются установки
компонентов — 507,10 из 12 073,31 мм³).

**Вся таблица на копиях, со всеми правками агентов в дереве: 220 из 220
поймано, 2992 с стены при 10 158 с прогонов наборов на четырёх копиях** —
набор сгибов 658 с в базовом прогоне и до 651 с на строку, машина всё это
время делилась с прогонами двух других проектов; ни одной отказанной,
зависшей или недоказанной строки. `run_all` после этого: 31/31 под
cadquery-ocp 7.9 (822 с) и 31/31 под 8.0 (898 с), на той же загруженной
машине.

Наборы выросли примерно на 1000 строк: `test_skill_pins` 88 → 186 строк
PASS, `test_mech` 20 → 29, `test_silk` 51 → 62, `test_pads` 108 → 112,
`test_bend` 229 → 234, наборы платы 195 → 210, `test_launch_cmd` 25 → 35,
`test_geom` 30 → 35; `tests/test_mutations.py` задаёт свои вопросы о 220
строках в 945 проверках. Полные отчёты четырёх агентов — инвентарь по
наборам, каждая строка с её строкой FAIL, замеры эквивалентности — лежат в
`build/agents/<область>/report.md`, вне репозитория.

### Что по-прежнему не покрыто после третьего прохода

- **Дуга через обёртку не обязана оставаться дугой**
  (`bend-arcs-wrapped-as-splines`, единственная открытая выжившая): с
  выключенной веткой `GeomAbs_Circle` в `_edge_curves` выемка подгоняется
  сплайном, и все объёмы всё равно сходятся до 1e-6, потому что ни в одной
  фикстуре нет той разгрузочной выемки, из-за которой OCC однажды объявил
  проволоку самопересекающейся на реальной плате. Закрыла бы проверка, что у
  обёрнутого тела остались круговые или эллиптические рёбра; каждая попытка
  стоит прогона набора сгибов.
- Одиннадцать строк по свёртке написаны и проверены на применимость, но не
  прогнаны за нехваткой машинного времени (`build/agents/bend/make_rows.py`):
  авто-якорь держит наименьший кусок, ломтики раньше панелей в
  `flat_frame`, не перевёрнутая проволока отверстия, инвертированный порог
  общих полос, полоса вдесятеро шире, непреобразованный дальний край стыка,
  порядок цепочки, ломтики как панели, проигнорированный локальный стек,
  `_piece_at` по дальнему, k вне развёрнутой длины. Ремонт защемления в
  `_piece_face` не исполняется никогда: защемление ничто не порождает.
- Ни один набор платы не трогает цвет торца и `_rim_faces`; предпочтение
  стека PRIMARY, ветки отказа `_layer_region` и защиты `build_contour` не
  объявлены ничем; у самозамкнутой дуги в `_pad_wire`,
  `StepFileIndex._same_root`, `models._sanitize` (все имена фикстур и так
  простые) и у полигона поверх двух замаскированных зон на *разной* высоте
  нет данных.
- **Ни один тест не исполняет SKILL.** Всё на той стороне — закрепление по
  исходнику плюс копия на Python; у тринадцати новых закреплённых процедур
  пока нет записанных ответов Allegro (`skill_answers.json` покрывает пять;
  случаи добавляются в `probe_translit.il`), а золотому корпусу по-прежнему
  нужен Allegro, и его нет в `run_all`. Что `S3D_NegativeLayers` и
  `S3D_ExportFullBoard` должны сбрасываться на каждый экспорт, не называет
  ничто.
- Зеркальный вывод со смещённым сверлом, ни разу не исполнявшаяся ветка отказа
  `s3dCollectExposed` и `run_all` без золотых корпусов — как и были.
